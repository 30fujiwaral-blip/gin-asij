Place the GIN hero image here and name it exactly:

  gin-hero.png

Then refresh your browser. If the image doesn’t appear, try clearing your cache or performing a hard reload.

Tip: You can use any image format (png/jpg/webp). If you use a different name, update styles.css background URL accordingly.
